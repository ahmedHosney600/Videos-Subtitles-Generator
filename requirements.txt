# Video → Subtitles — Python Dependencies
# Install with: pip install -r requirements.txt
#
# IMPORTANT: Also install ffmpeg before running:
#   Mac: brew install ffmpeg
#   Linux/Colab: apt-get install ffmpeg

# Core transcription engines (OS dependent)
mlx-whisper ; sys_platform == 'darwin'
faster-whisper ; sys_platform == 'linux'
ctranslate2 ; sys_platform == 'linux'

# Audio/video processing
ffmpeg-python

# Rich terminal UI
rich
